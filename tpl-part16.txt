## Part 16: Master Roadmap (Extensions)

> **Status: NON-NORMATIVE ROADMAP (effort estimates are guesses). Moved to `docs/aux/roadmap.md`.**

Quick overview (full content → aux):

```
Phase 0..6  (core)  ✓ covered by v3
   ↓
   ├──→ Phase S1..S5  Space-grade research backends (NOT flight software; NOT Phase 0–6 Final deliverable)
   └──→ Phase X1..X2  Cross-platform / Cross-architecture
```

| Track | Effort | Target |
|-------|--------|--------|
| 0 | 4 w | yoyo |
| 1 | 6 w | yoyo |
| 2 | 8 w | yoyo-js + yoyo-rust |
| 3 | 2 w | yoyo |
| 4 | 4 w | yoyo |
| 5 | 6 w | yoyo |
| 6 | 4 w | yoyo (docs/) |
| S1 | 12 w | yoyo |
| S2 | 12 w | yoyo |
| S3 | 12 w | yoyo |
| S4 | 24 w | yoyo + lab |
| S5 | varies | mission-specific |
| X1 | 8 w / OS | yoyo |
| X2 | 24-48 w / arch | yoyo |

**Total core (0-6)**: ~34 weeks (~8 months, 1 person). **Total with space (0-S4)**: ~94 weeks (~22 months, 1-3 people). **Total with cross-arch**: 118-142 weeks (~3 years, 2-5 people).

**Critical path** (ROADMAP only): 88 weeks (~20 months) to a *space-oriented research posture*, solo — **not** "mission-ready / Space-Ready" branding. **Optimal**: 50-60 weeks with 3 people.

**First deliverable (in-scope for Phase 0–6)**: A frozen, self-hosting compiler with ~2,300 lines total under Part L honesty.
**ROADMAP vision (Part 15/16 — NOT Phase 0–6 Final deliverable)**: space-oriented backends and radiation *testing campaigns* if funded. Part L still: YOYO is **not** space-grade flight software under this spec.

### 16.9 Build & Test Commands

```bash
cd yoyo-rust && cargo build --release
cargo test              # all tests
cargo test primitives   # Phase 0
cargo test isa_table    # Phase 1
cargo test ddc          # Phase 2
cargo test variable     # Phase 3
cargo test platform     # Phase 4
cargo test baremetal    # Phase 5
cargo clippy --all-targets --all-features -- -D warnings
cargo fmt --check

# 3-Chain DDC
node yoyo-js/src/yoyo.js yoyo/projects/yoyo.ty /tmp/yoyo_js.exe
cd yoyo-rust && ./target/release/yoyo link ../yoyo/projects/yoyo.ty /tmp/yoyo_rust.exe
cd yoyo-asm && ./yoyo-asm ../yoyo/projects/yoyo.ty /tmp/yoyo_asm.exe
sha256sum /tmp/yoyo_*.exe   # code + data must match

# QEMU bare-metal
./target/release/yoyo link --target=baremetal kernel.ty kernel.elf
qemu-system-x86_64 -kernel kernel.elf -nographic -serial mon:stdio
```

Golden hash verification (3 anchors), lockdown verification (`scripts/verify-yoyo-ty.mjs` + `verify-selfhost.ps1` + `yoyo diff`) — full recipes → `docs/aux/build.md` §"Build + test".