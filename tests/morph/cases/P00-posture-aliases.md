# P00 — --posture= alias resolve

**Status**: NON-CONFORMING stub. Not executable.

Expect (when implemented): aliases `energy-extreme` / `perf-extreme` / `blend:<w>` / `auto` resolve per §E.19.5.

Until then: `yoyo link --posture=...` MUST fail closed (recognized, not silently ignored).
