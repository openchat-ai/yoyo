---
description: Stage 4 零指令自动负责人 — AUTO-TO-1.0 无人值守直到 1.0
alwaysApply: true
---

# Stage 4 自动负责人（零指令 + AUTO-TO-1.0）

本仓库中，**除非** `AUTO_TO_1.0.md` 为 `ACTIVE=0`，或用户明确说 `停` / 「只讨论/不要动手」，否则按自动负责人执行。

**长期北星**：`SCOPE-v1.0.md` / `ROADMAP-TO-1.0.md` → **1.0**（打破后门魔咒 · DDC+Lock 扩面 / 缩宿主信任）。DDC = detection；**禁止** Thompson-proof；**禁止**发明 ROADMAP 外无界功能。

---

## 0. 每次 wake 最先读开关

```text
F:\yoyo\AUTO_TO_1.0.md
```

| `ACTIVE` | 模式 |
|----------|------|
| `1` | **AUTO-TO-1.0**：无人值守；见下节 |
| `0` 或不存在且无触发词 | 普通零指令（`继续` 等才推进） |

用户已说过 **自动持续 / 推到1.0 / 离电脑** 且 flag=`ACTIVE=1` 时：**不要**向用户索取「继续」；**不要**教用户打字。

---

## AUTO-TO-1.0 模式（`ACTIVE=1`）

### 何时执行

下列任一即 **loop tick**，必须立刻做下一未勾 Stage 项（无确认、无 UX 提示）：

- 任意 agent wake / 会话回合开始且本模式激活  
- 空消息 / `继续` / `开工` / `auto` / `自动` / `推到1.0` / `持续` / loop 定时 tick  
- coordinator 完成一项后若仍 `ACTIVE=1` 且会话可续：同会话或下一 tick 继续下一项  

### 停止条件（仅此）

1. 用户说 **`停`**，或把 `AUTO_TO_1.0.md` 改为 **`ACTIVE=0`**
2. **Hard block**：需人类（密钥/账号/不可替代硬件/政策禁止 SCOPE-CUT）——写清原因到当前看板「阻塞」节，停执行；**不**假装完成
3. 已达 **v1.0 毕业**（RELEASE + tag 完成）→ 可将 flag 标完成说明；默认停新功能

### Stage 边界：自动「定」下一版

当当前 Stage **全部 `[x]`**（含毕业 D 完成后）：

1. 读 `ROADMAP-TO-1.0.md` 下一版本行  
2. **自动「定」**：若不存在则创建 `SCOPE-v0.N.md`（或 v1.0 GRAD）+ `STAGEk_OWNER_CHECKLIST.md`（按 ROADMAP 信任主题生成 ≤5 门，模板对齐 STAGE10）  
3. 更新 ROADMAP 状态行；Glass 打开新看板  
4. **立刻**执行新看板第一未勾项  
5. **不等**用户口头「定 0.N」

### 毕业 D：auto release（对齐 v0.1–v0.3）

当 Stage **D** 验收全绿并勾选后，agent **必须**（无需再问用户）：

1. 写/更新 `RELEASE-vX.Y.md`  
2. **git commit**（毕业相关文件）  
3. **git tag**（如 `v0.4.0`）  
4. **push** commit + tag  
5. **GitHub Release**（`gh release create`，body 用 RELEASE 摘要）  

**非毕业 WIP：禁止 push。** 日常项默认可不 commit，除非该门验收要求落盘；毕业除外。

### 每 tick 工作量

- 默认：**一项** Stage checkbox（实现+验收+勾选）  
- `继续 两板`：仍走两板并行协议  
- 禁止一次空想多版功能

---

### 触发分流（必须先解析）

| 用户输入 | 模式 | 行为 |
| -------- | ---- | ---- |
| `停` / 只讨论/不要动手 | **停** | 可写 `ACTIVE=0`；不改代码 |
| `维护` | **维护** | 只回归；不新开信任门（AUTO 下若无 hard block 则维护后继续下一项） |
| `继续 两板` / `继续两板` | **两板** | 见「两板协调」 |
| `ACTIVE=1` 下其它 / 空 / loop | **AUTO 单轨** | 下一未勾项，不问 |

---

## 两板并行（`继续 两板`）

**目标**：历史 Container DDC PE+ELF 两路；仅 coordinator 更新看板。

0. MCP 打开当前主线看板  
1. `& F:\yoyo\scripts\stage4-two-board.ps1`  
2. 并行 background：PE=`F:\yoyo-worktrees\stage4-pe`；ELF=`F:\yoyo-worktrees\stage4-elf`  
3. 两路 `cargo run -- test ddc` 绿 → `& F:\yoyo\scripts\stage4-two-board-merge.ps1` → master 再验 → 勾  
4. 仅一路绿：汇报；不勾；不 merge  

PE/ELF **不得**改 master 看板。AUTO 下两板完成后若仍有未勾项且 `ACTIVE=1`，继续下一项。

---

## 每次响应前必做

**-1. 读 `AUTO_TO_1.0.md`**（决定是否 AUTO）

0. **右侧打开当前主线看板**（读文件前）：
   - MCP `cursor-app-control` → `open_resource`
   - **post-v1.0 关洞**（`ACTIVE=0` · 用户 `继续`/`关洞`）：`file:///F:/yoyo/POST-1.0-HOLE-CHECKLIST.md`
   - **历史 Stage**（只读 · 勿回改）：`STAGE16_OWNER_CHECKLIST.md`（v1.0 已毕业）
   - 更早 Stage：`STAGE*_OWNER_CHECKLIST.md`（4→15 已毕业则跳过）
   - 失败不阻塞

1. **读看板**；历史 Stage 勾选勿回改  

2. **选下一项**（跳过 `[x]`）：
   - **post-v1.0**（`ACTIVE=0` · `COMPLETED=1` · 用户 `继续`/`关洞`）：读 `POST-1.0-HOLE-CHECKLIST.md` → A→B→C 下一未勾；**禁止** invent Stage 17  
   - Stage 4→16 / v1.0：已毕业 → 跳过（勿回改 STAGE16）  
   - **AUTO**（仅 `ACTIVE=1`）：Stage 4→16 历史逻辑；v1.0 已毕业则 **停**（见 Post-v1.0 节）  
   - hard block → 写入看板「阻塞」节  

3. **执行** + 验收（PowerShell）：
   ```powershell
   cd F:\yoyo\yoyo-rust\verifier
   cargo run -- test ddc
   ```
   加跑看板该项要求的 golden/backends/lock/selfhost 等  

4. **绿了才勾**；同步毕业判定 / `BACKEND_SUPPORT.md`（若语义变）  

5. **Commit/push**：默认否；**毕业 D** 按 AUTO release 强制 commit+tag+push+gh；WIP 不 push  

6. **冲突**：他 agent 正在改 Stage 10-B/C 时，本会话若非认领 B/C 则勿覆盖其实现；可做文档/规则/其它未认领项，或等 tick 认领同一未勾项时接续  

---

## 汇报格式（给 coordinator / 时间线，非教用户操作）

- 执行了哪一项；命令与绿/红；勾了哪框  
- 若 auto「定」了新版本：写出新建 SCOPE/STAGE 路径  
- 若毕业 release：写出 tag / Release URL  
- 若 hard block：一句话原因 + 需要人类做什么  
- **禁止**写「请用户说继续」类 UX  

---

## 当前待办速查（以看板为准）

| 项 | 主题 |
|----|------|
| S10–S16 | v0.4→v1.0 已毕业（跳过 · 勿回改） |
| **post-v1.0** | `POST-1.0-HOLE-CHECKLIST.md` — path 2 关洞 A→B→C |
| post-A | Win OW-IAT wire-up smoke GREEN（**阻塞：with-sidecar AV**） |
| post-B | Linux OW-IAT / tramp 回归 |
| post-C | SCOPE-CUT + BACKEND_SUPPORT 诚实 sync |

## Post-v1.0 关洞（`ACTIVE=0` · 用户 `继续`/`关洞`）

v1.0 / Stage 16 已毕业；`AUTO_TO_1.0.md` **`ACTIVE=0` · `COMPLETED=1`** → **勿启 AUTO invent Stage 17**。

| 触发 | 行为 |
| ---- | ---- |
| `继续` / `关洞` / `post-1.0` / `path 2` | 读 `POST-1.0-HOLE-CHECKLIST.md`；**一项** A→B→C per tick |
| 验收 | 本地先跑看板该项 PowerShell 门禁；绿了才勾 |
| push | 遵守 **CI anti-thrash**（`.cursor/rules/ci-anti-thrash.mdc`）：本地 smoke 先绿 → 一次 CI push；WIP `[skip ci]` |
| 诚实 | OW-IAT smoke GREEN **≠** CLOSED；sidecar 仍在则 CUT |
| 阻塞 | 写入看板「阻塞」节；with-sidecar AV → 本地 bisect（`H00_BISECT` opt-in） |

**下一项（2026-08-31）** = **A** — `& .\scripts\stage17-ow-iat-wireup.ps1`（with-sidecar RED）。

## 约束

- 最小 diff；Rust 链为主  
- PowerShell 用 `&`，忌 `Start-Process \| Out-Null`  
- 红了修到绿再勾  
- 北星 = 打破后门魔咒  
- 不发明无界功能；不宣称 Thompson-proof  
- **CI anti-thrash**（详规 `.cursor/rules/ci-anti-thrash.mdc`）：CI 是 gate 不是 debugger；本地先 smoke；同 PR 连续 2 次红全量 CI（或 3 次无新证据的 stub 微调 push）则停推；WIP 用 `[skip ci]`；勿并行开 CI；勿清 failed Actions 假装绿；**允许** auto-fix，只限 thrash  
