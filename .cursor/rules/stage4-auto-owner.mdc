---
description: Stage 4 零指令自动负责人 — 读看板、执行下一项、验收、勾框
alwaysApply: true
---

# Stage 4 自动负责人（零指令）

本仓库中，**除非用户明确说「只讨论/只解释/不要动手」**，收到任意消息时均视为可能触发自动负责人模式。

触发词（非必须）：`继续`、`开工`、`auto`、`stage4`、空消息、或任何模糊指令。

### 触发分流（必须先解析）

| 用户输入 | 模式 | 行为 |
| -------- | ---- | ---- |
| `继续 两板` / `继续两板` | **两板并行** | 见下节「两板协调」；**不要**在 master 上单轨做 B |
| `继续` / `开工` / `auto` / 其它 | **单轨** | master 上按 A→B→C 下一未勾项顺序执行 |

解析规则：消息含 `两板`（可带空格，如 `继续 两板`）→ 两板模式；否则单轨。

## 两板并行（`继续 两板`）

**目标**：B 项 Container DDC 拆成 PE + ELF 两路并行，仅 **coordinator（当前 master 会话）** 更新看板。

### Coordinator 必做顺序

0. MCP 右侧打开看板（同单轨）
1. 运行 worktree 脚本（缺则创建）：
   ```powershell
   & F:\yoyo\scripts\stage4-two-board.ps1
   ```
2. **同时**启动两个 **background** 子 agent（`subagent_type=best-of-n-runner` 或 `generalPurpose`，`run_in_background=true`）：
   - **Agent PE** — 工作目录 `F:\yoyo-worktrees\stage4-pe`，分支 `stage4/container-pe`
   - **Agent ELF** — 工作目录 `F:\yoyo-worktrees\stage4-elf`，分支 `stage4/container-elf`
3. 等两路均汇报 **绿**（各自 worktree 内 `cargo run -- test ddc`，container 对应路径 PASS）
4. 仅当 **两路都绿**：在 **master**（`F:\yoyo`）运行：
   ```powershell
   & F:\yoyo\scripts\stage4-two-board-merge.ps1
   ```
   合并成功后 master 上 `cargo run -- test ddc` 全绿 → 勾 B `[x]`，必要时改 `BACKEND_SUPPORT.md`
5. **仅一路绿**：汇报 PE/ELF 各自状态；**不勾 B**；不 merge

### 子 agent 约束（写进 launch prompt）

| Agent | 范围 | 禁止 |
| ----- | ---- | ---- |
| PE | PE32+ x64 container NOP+RET 最小实现；验收侧重 container PE 路径 | 改 ELF；改 `STAGE4_OWNER_CHECKLIST.md`；在 master 上改文件 |
| ELF | ELF64 x64 container NOP+RET 最小实现；验收侧重 container ELF 路径 | 改 PE；改看板；在 master 上改文件 |

子 agent 可在各自分支 **commit**（用户未禁止时）；默认 **不 push**。

### 看板编辑权

- **仅 coordinator 在 master** 修改 `STAGE4_OWNER_CHECKLIST.md` 与 `BACKEND_SUPPORT.md`
- PE/ELF worktree 内 agent **不得**勾选或编辑看板

## 每次响应前必做

0. **右侧打开看板**（**在读文件之前**，本步为自动负责人首动作）：
   - 调用 MCP `cursor-app-control` → `open_resource`
   - URI 固定：`file:///F:/yoyo/STAGE4_OWNER_CHECKLIST.md`
   - 在 Glass **右侧面板**打开看板；失败不阻塞，继续后续步骤
   - **Hooks 做不到**：`workspaceOpen` / `sessionStart` 无法打开 Glass 右栏，仅 Agent 规则 + MCP 可实现
1. **读取看板**：`STAGE4_OWNER_CHECKLIST.md`（完整路径 `F:\yoyo\STAGE4_OWNER_CHECKLIST.md`）
2. **选下一项**（严格顺序，跳过已 `[x]`）：
   - Stage 4 毕业三门：**A → B → C**（见「Stage 4 毕业三门」）
   - A/B/C 全 `[x]` 后 → **Stage 5** 预置任务第一项
   - Stage 5 全 `[x]` 后 → **Stage 6** 预置任务第一项
   - Stage 6 全 `[x]` 后 → **Stage 7** 预置任务第一项
3. **执行该项**：实现 + 验收；看板「对 AI 说什么」话术为任务规格，用户**不必**点名 A/B/C
4. **验收命令**（PowerShell）：
   ```powershell
   cd F:\yoyo\yoyo-rust\verifier
   cargo run -- test ddc
   ```
   - 任务涉及 golden/backends/lock/selfhost 时加跑对应命令（见看板该项验收）
5. **绿了才勾框**：仅当验收通过，将看板对应 `- [ ]` 改为 `- [x]`，并同步「毕业判定」行与 `BACKEND_SUPPORT.md`（若语义变）
6. **默认不 push、不 commit**（除非用户明确要求 commit）

## 汇报格式（简洁）

用户是验收负责人，不需要底层细节。仅报告：

- 执行了哪一项（A/B/C 或 Stage 5/6/7 子项名称）
- 跑了什么命令、退出码 / 绿或红
- 勾了哪个 checkbox
- 若 blocked：一句话原因 + 需要用户做什么

**不要**在未跑验收前勾选；**不要**跳过 B 去做 C（C 依赖 B）。

## 当前待办速查（以看板文件为准）

看板文件是唯一真相；下列仅为规则内提醒，**每次仍须读文件**：

| 项 | 主题 |
|----|------|
| A | Win/Linux MEMCPY_STATE slot-form + core fatal |
| B | Container DDC（PE+ELF NOP+RET，取消 SKIP） |
| C | LDB 指针内存 DDC + container 路径 |
| S7-1 | Stage 7 维护节拍（test all + win selfhost + lock + JS golden） |
| S7-2 | JS golden MEMCPY 真实 emit（去 stub） |
| S7-3 | Windows 无 sidecar 自举 |
| S7-4 | Linux M2→M3 自举链 |
| S7-5 | 定制 MCU 后端脚手架 |

## 约束

- 最小 diff；Rust 链为主（`yoyo-rust/verifier`）
- PowerShell：用 `&` 直接调用，避免 `Start-Process | Out-Null` pipe 误判退出码
- 红了：修到绿再勾，不把半成品标完成
