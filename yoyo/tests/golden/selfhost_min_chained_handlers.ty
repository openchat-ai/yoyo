; W-selfhost-min golden (chained) — combined H_05 + H_06 emit → x64 90 C3 90 90 C3
; H_05 (raw NOP + RET) followed by H_06 (two raw NOPs + RET).
; This exercises a two-handler minimal sequence in canonical yoyo.ty source order.
; Per-peer golden for the combined sequence is Rust-only (compile_one_handler
; for both H_05 and H_06, then concatenated) per W-selfhost-min chained beat.
40 05
  A0 90
  FF
40 06
  A0 90
  A0 90
  FF
